---
import type { CollectionEntry } from "astro:content";
import readingTime from "reading-time";

interface Props {
	post: CollectionEntry<"blog">;
}
const { post } = Astro.props;
const time = readingTime(post.body ?? "");
---

<a class="flex mx-8 my-4 text-sm" href={`/blog/${post.id}`}>
	<div class="flex flex-col justify-start items-start gap-1">
		<span class="ml-8 text-xl font-bold">{post.data.title}</span>
		<span class="ml-8">Published {post.data.date}</span>
		<span class="ml-8">{time.text}</span>
	</div>
</a>
